import React from "react";
import Typography from "@material-ui/core/Typography";
import { makeStyles, Theme } from "@material-ui/core/styles";

const pats = () => {
  return (
    <div>
      <div>
        <Typography variant="h1">Patient Management</Typography>
      </div>
    </div>
  );
};

export default pats;